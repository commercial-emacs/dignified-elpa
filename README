Create .github/workflows/release.yml in your package repository:

name: Release
on:
  release:
    types: [published]

jobs:
  build:
    uses: commercial-emacs/dignified-elpa/.github/workflows/build.yml@v1
    with:
      files: "your-package.el dir/other.el README"
